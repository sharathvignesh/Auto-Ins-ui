import { Layout } from 'antd';
import React from 'react';

const { Header } = Layout;
const AppHeader: React.FC = () => (
  <>
    <Header className="appHeader">Auto insurance premium calculator</Header>
  </>
);

export default AppHeader;
